# Local TLS certificates / private keys (e.g. mkcert dev certs). Never commit keys.
.certs/

.venv

.DS_Store
# Local generator/library clones (consumed via go.mod replace / local plugin builds)
orm/
grpc-mcp-gateway/
