// Code generated by protoc-gen-protorm. DO NOT EDIT.
// versions:
// 	protoc-gen-protorm 0.1.4
// 	protoc             (unknown)
// source: freebusy/promocode/v1/promocode.proto
//
// database: v1
// schemas:  freebusy_ohtarnished_dev
//
// protorm — https://github.com/the-protobuf-project/protorm

/// How a promo code's discount is computed.
enum DiscountType {
  /// A percentage off the subtotal (percent_off).
  PERCENTAGE

  /// A fixed amount off the subtotal (amount_off).
  FIXED_AMOUNT

  /// Maps the enum to the "discount_type" type in the database.
  @@map("discount_type")
  /// Maps the enum to the "freebusy_ohtarnished_dev" schema in the database.
  @@schema("freebusy_ohtarnished_dev")
}

/// Lifecycle state of a promo code.
enum State {
  /// Redeemable (subject to window and caps).
  ACTIVE

  /// Manually disabled.
  DISABLED

  /// Past its redemption window or out of redemptions.
  EXPIRED

  /// Maps the enum to the "state" type in the database.
  @@map("state")
  /// Maps the enum to the "freebusy_ohtarnished_dev" schema in the database.
  @@schema("freebusy_ohtarnished_dev")
}

/// A redeemable discount applied to a booking's subtotal. Scoped by a redemption window, usage caps, a minimum subtotal, and an optional set of resources / offerings it applies to.
model PromoCode {
  /// Unique identifier for the record.
  id String @id @default(ulid()) @map("id")

  /// The promo code name. Format: promoCodes/{promo_code}
  name String @unique @map("name")

  /// The human-entered code, unique across all promo codes (e.g. "SUMMER25").
  code String @map("code")

  /// Internal display name (not shown to customers).
  displayName String? @map("display_name")

  /// Free-form description.
  description String? @map("description")

  /// Whether the discount is a percentage or a fixed amount.
  discountType DiscountType @default(PERCENTAGE) @map("discount_type")

  /// Percentage off (1-100), when discount_type is PERCENTAGE.
  percentOff Int? @map("percent_off")

  /// Fixed amount off, when discount_type is FIXED_AMOUNT.
  amountOff Json? @map("amount_off")

  /// Earliest the code can be redeemed. Unset means no lower bound.
  redeemStartTime DateTime? @map("redeem_start_time")

  /// Latest the code can be redeemed. Unset means no upper bound.
  redeemEndTime DateTime? @map("redeem_end_time")

  /// Maximum total redemptions across all customers. Zero means unlimited.
  maxRedemptions BigInt? @map("max_redemptions")

  /// Maximum redemptions per customer. Zero means unlimited.
  perCustomerLimit Int? @map("per_customer_limit")

  /// Minimum subtotal required for the code to apply.
  minSubtotal Json? @map("min_subtotal")

  /// Resources the code applies to. Empty means all resources. Format: resources/{resource} TODO: unresolved reference to Resource (not in this generation set); kept as a plain indexed column.
  applicableResources String[] @map("applicable_resources")

  /// Offerings the code applies to. Empty means all offerings. Format: resources/{resource}/offerings/{offering} TODO: unresolved reference to Offering (not in this generation set); kept as a plain indexed column.
  applicableOfferings String[] @map("applicable_offerings")

  /// How many times the code has been redeemed.
  redemptionCount BigInt? @map("redemption_count")

  /// Derived lifecycle state: ACTIVE, DISABLED (when `disabled` is set), or EXPIRED (past the window or out of redemptions).
  state State? @map("state")

  /// If true, the code is manually disabled regardless of its window and caps.
  disabled Boolean? @map("disabled")

  /// Creation timestamp.
  createTime DateTime @default(now()) @map("create_time")

  /// Last-modification timestamp.
  updateTime DateTime @updatedAt @map("update_time")

  /// Opaque version for optimistic concurrency (AIP-154); echo on update/delete.
  etag String? @map("etag")

  /// Composite index on [applicable_resources].
  @@index([applicableResources])
  /// Composite index on [applicable_offerings].
  @@index([applicableOfferings])
  /// Maps the model to the "promo_codes" table in the database.
  @@map("promo_codes")
  /// Maps the model to the "freebusy_ohtarnished_dev" schema in the database.
  @@schema("freebusy_ohtarnished_dev")
}
