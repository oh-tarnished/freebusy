// Code generated by protoc-gen-orm. DO NOT EDIT.
// versions:
// 	protoc-gen-orm 1.2.0
// 	protoc (unknown)
//
// database: freebusy
// schemas:  booking, identity, organisation, promocode, resource, schedule, shared, common
//
// Prisma 7 configuration; connection URLs are environment-driven.
//
// orm — https://github.com/the-protobuf-project/orm

import { resolve } from "node:path";
import { config } from "dotenv";
import { defineConfig, env } from "prisma/config";

// Prisma 7 no longer auto-loads .env, so load it here before env() is read.
config({ path: resolve(__dirname, ".env") });

export default defineConfig({
	// Point to the folder holding every .prisma file; subdirectories are
	// discovered automatically, so all schemas load from one config.
	schema: resolve(__dirname),
	migrations: {
		path: resolve(__dirname, "migrations"),
	},
	datasource: {
		// Single database URL for all schemas.
		// Declared in proto: postgresql://user:pass@localhost:5432/freebusy
		url: env("FREEBUSY_DATABASE_URL"),
	},
});
