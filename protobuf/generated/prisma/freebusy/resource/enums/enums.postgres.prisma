// Code generated by protoc-gen-orm. DO NOT EDIT.
// versions:
// 	protoc-gen-orm 1.2.0
// 	protoc (unknown)
// source: freebusy/resource/v1/enums.proto
//
// database: freebusy
// schemas:  resource
//
// orm — https://github.com/the-protobuf-project/orm

/// Kind of bookable resource.
enum ResourceType {
  /// A person who delivers a service (e.g. a doctor, stylist).
  PROVIDER

  /// A bookable room or space (e.g. a meeting room).
  ROOM

  /// A bookable piece of equipment (e.g. a kayak).
  EQUIPMENT

  /// A lodging unit type backed by a pool of identical units.
  UNIT_TYPE

  /// A generic space or venue.
  SPACE

  /// Maps the enum to the "resource_type" type in the database.
  @@map("resource_type")
  /// Maps the enum to the "resource" schema in the database.
  @@schema("resource")
}

/// Lifecycle status of a resource.
enum ResourceState {
  /// Bookable.
  ACTIVE

  /// Retired; hidden from availability and new bookings.
  ARCHIVED

  /// Maps the enum to the "resource_state" type in the database.
  @@map("resource_state")
  /// Maps the enum to the "resource" schema in the database.
  @@schema("resource")
}

/// What an offering's price is charged per.
enum PricingUnit {
  /// A flat price for the whole booking.
  PER_BOOKING

  /// Price multiplied by the number of nights (NIGHTLY resources).
  PER_NIGHT

  /// Price multiplied by party size / units booked.
  PER_PERSON

  /// Maps the enum to the "pricing_unit" type in the database.
  @@map("pricing_unit")
  /// Maps the enum to the "resource" schema in the database.
  @@schema("resource")
}

/// Lifecycle state of an offering.
enum OfferingState {
  /// Bookable.
  ACTIVE

  /// Hidden from new bookings.
  INACTIVE

  /// Maps the enum to the "offering_state" type in the database.
  @@map("offering_state")
  /// Maps the enum to the "resource" schema in the database.
  @@schema("resource")
}
