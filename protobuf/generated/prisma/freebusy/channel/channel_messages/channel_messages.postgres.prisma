// Code generated by protoc-gen-orm. DO NOT EDIT.
// versions:
// 	protoc-gen-orm 1.3.1
// 	protoc (unknown)
// source: freebusy/channel/v1/channel_messages.proto
//
// database: freebusy
// schemas:  channel
//
// orm — https://github.com/the-protobuf-project/orm

/// A rollup of a channel's sync health, modeled as a singleton sub-resource of the channel (one per channel) and read via GetChannelSyncStatus.
model ChannelSyncStatus {
  /// Unique identifier for the record.
  id String @id @default(ulid()) @map("id")

  /// The sync-status name. Format: channels/{channel}/syncStatus
  name String @unique @map("name")

  /// Current connection state.
  state ChannelState? @map("state")

  /// When ARI/reservations last synced successfully.
  lastSyncTime DateTime? @freebusy.Timestamptz(6) @map("last_sync_time")

  /// Count of updates still queued.
  pendingCount Int? @map("pending_count")

  /// Count of updates that failed since the last success.
  failedCount Int? @map("failed_count")

  /// Most recent error message, when the last sync failed.
  lastError String? @map("last_error")

  /// Maps the model to the "sync_statuses" table in the database.
  @@map("sync_statuses")
  /// Maps the model to the "channel" schema in the database.
  @@schema("channel")
}
