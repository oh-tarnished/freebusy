// Code generated by protoc-gen-protorm. DO NOT EDIT.
// versions:
// 	protoc-gen-protorm 1.1.1
// 	protoc             (unknown)
// source: freebusy/identity/v1/identity.proto
//
// database: freebusy
// schemas:  identity
//
// protorm — https://github.com/the-protobuf-project/protorm

/// A signed-in person. Identity is deliberately thin: actual login is an OIDC redirect flow handled over plain HTTP by the IdP, so most of "auth" never appears as an RPC. Email and identity come from the IdP and are read-only here; only profile preferences are editable.
model User {
  /// Unique identifier for the record.
  id String @id @default(ulid()) @map("id")

  /// The user name. The alias "users/me" resolves to the caller. Format: users/{user}
  name String @unique @map("name")

  /// Email address, sourced from the IdP.
  email String? @map("email")

  /// Display name.
  displayName String? @map("display_name")

  /// Avatar image URL.
  avatarUrl String? @map("avatar_url")

  /// BCP 47 locale (e.g. "en-US").
  locale String? @map("locale")

  /// IANA time zone (e.g. "America/New_York").
  timeZone String? @map("time_zone")

  /// Creation timestamp.
  createTime DateTime @default(now()) @freebusy.Timestamptz(6) @map("create_time")

  /// Last-modification timestamp.
  updateTime DateTime @updatedAt @freebusy.Timestamptz(6) @map("update_time")

  /// Opaque version for optimistic concurrency (AIP-154); echo on update.
  etag String? @map("etag")

  /// Back-relation: Booking records that reference this model via customer.
  resource Booking[]

  /// Back-relation: MembershipSummary records that reference this model via user_id.
  membershipSummaries MembershipSummary[]

  /// Back-relation: Member records that reference this model via user.
  members Member[] @relation("MemberUser")

  /// Back-relation: Member records that reference this model via inviter.
  members2 Member[] @relation("MemberInviter")

  /// Back-relation: Redemption records that reference this model via customer.
  redemptions Redemption[]

  /// Maps the model to the "users" table in the database.
  @@map("users")
  /// Maps the model to the "identity" schema in the database.
  @@schema("identity")
}

/// A compact view of an organisation the user belongs to.
model MembershipSummary {
  /// Unique identifier for the record.
  id String @id @default(ulid()) @map("id")

  /// The organisation. Format: organisations/{organisation}
  organisationID String? @map("organisation")

  /// Relation to Organisation via organisation.
  organisation Organisation? @relation(fields: [organisationID], references: [id])

  /// Cached display name of the organisation.
  orgDisplayName String? @map("org_display_name")

  /// The user's role in the organisation (an OrganisationRole value name).
  role String? @map("role")

  /// Foreign key to User.
  userID String @map("user_id")

  /// Relation to User via user_id.
  user User @relation(fields: [userID], references: [id], onDelete: Cascade)

  /// Composite index on [organisation].
  @@index([organisationID])
  /// Composite index on [user_id].
  @@index([userID])
  /// Maps the model to the "membership_summaries" table in the database.
  @@map("membership_summaries")
  /// Maps the model to the "identity" schema in the database.
  @@schema("identity")
}
