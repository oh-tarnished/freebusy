// Code generated by protoc-gen-orm. DO NOT EDIT.
// versions:
// 	protoc-gen-orm 1.3.0
// 	protoc (unknown)
// source: freebusy/identity/v1/enums.proto
//
// database: freebusy
// schemas:  identity
//
// orm — https://github.com/the-protobuf-project/orm

/// A guest's gender, as recorded on a registration card.
enum Gender {
  /// Male.
  MALE

  /// Female.
  FEMALE

  /// Another gender identity.
  OTHER

  /// Declined to state.
  UNDISCLOSED

  /// Maps the enum to the "gender" type in the database.
  @@map("gender")
  /// Maps the enum to the "identity" schema in the database.
  @@schema("identity")
}

/// Which age bracket a guest falls in. Drives occupancy counting and child pricing; may be given explicitly or derived from date_of_birth.
enum AgeGroup {
  /// An adult (occupies an adult slot; charged the adult rate).
  ADULT

  /// A child (may be charged a child rate or stay free per policy).
  CHILD

  /// An infant (typically not counted against occupancy).
  INFANT

  /// Maps the enum to the "age_group" type in the database.
  @@map("age_group")
  /// Maps the enum to the "identity" schema in the database.
  @@schema("identity")
}

/// The kind of government identity document a guest presents at check-in.
enum IdDocumentType {
  /// Passport (required for foreign nationals, e.g. India Form C).
  PASSPORT

  /// A national identity card.
  NATIONAL_ID

  /// A driving licence.
  DRIVING_LICENSE

  /// India's Aadhaar card.
  AADHAAR

  /// A voter identity card.
  VOTER_ID

  /// Any other accepted document.
  OTHER

  /// Maps the enum to the "id_document_type" type in the database.
  @@map("id_document_type")
  /// Maps the enum to the "identity" schema in the database.
  @@schema("identity")
}

/// A guest's smoking preference for room assignment.
enum SmokingPreference {
  /// Prefers a non-smoking room.
  NON_SMOKING

  /// Prefers a smoking room.
  SMOKING

  /// Maps the enum to the "smoking_preference" type in the database.
  @@map("smoking_preference")
  /// Maps the enum to the "identity" schema in the database.
  @@schema("identity")
}

/// A guest's bed-type preference for room assignment.
enum BedPreference {
  /// No particular preference.
  NO_PREFERENCE

  /// A king bed.
  KING

  /// A queen bed.
  QUEEN

  /// Two twin beds.
  TWIN

  /// A single bed.
  SINGLE

  /// Maps the enum to the "bed_preference" type in the database.
  @@map("bed_preference")
  /// Maps the enum to the "identity" schema in the database.
  @@schema("identity")
}
