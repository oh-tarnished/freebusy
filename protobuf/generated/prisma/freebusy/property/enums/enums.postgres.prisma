// Code generated by protoc-gen-orm. DO NOT EDIT.
// versions:
// 	protoc-gen-orm 1.2.0
// 	protoc (unknown)
// source: freebusy/property/v1/enums.proto
//
// database: freebusy
// schemas:  property
//
// orm — https://github.com/the-protobuf-project/orm

/// Lifecycle status of a property.
enum PropertyState {
  /// Live and visible to guests.
  ACTIVE

  /// Retired; hidden from storefront and new bookings.
  ARCHIVED

  /// Maps the enum to the "property_state" type in the database.
  @@map("property_state")
  /// Maps the enum to the "property" schema in the database.
  @@schema("property")
}

/// Kind of bookable unit.
enum UnitType {
  /// A person who delivers a service (e.g. a doctor, stylist).
  PROVIDER

  /// A bookable room or space (e.g. a hotel room, meeting room).
  ROOM

  /// A bookable piece of equipment (e.g. a kayak).
  EQUIPMENT

  /// A lodging unit type backed by a pool of identical units (e.g. a room type).
  LODGING

  /// A generic space or venue.
  SPACE

  /// Maps the enum to the "unit_type" type in the database.
  @@map("unit_type")
  /// Maps the enum to the "property" schema in the database.
  @@schema("property")
}

/// What a unit's price is charged per.
enum PricingUnit {
  /// A flat price for the whole booking.
  PER_BOOKING

  /// Price multiplied by the number of nights (NIGHTLY units).
  PER_NIGHT

  /// Price multiplied by party size / units booked.
  PER_PERSON

  /// Maps the enum to the "pricing_unit" type in the database.
  @@map("pricing_unit")
  /// Maps the enum to the "property" schema in the database.
  @@schema("property")
}

/// Lifecycle status of a unit.
enum UnitState {
  /// Bookable.
  ACTIVE

  /// Retired; hidden from availability and new bookings.
  ARCHIVED

  /// Maps the enum to the "unit_state" type in the database.
  @@map("unit_state")
  /// Maps the enum to the "property" schema in the database.
  @@schema("property")
}
