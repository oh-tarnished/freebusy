components:
  schemas:
    freebusy.organisation.v1.InviteMemberArgs:
      description: Arguments for the "invite_member" prompt.
      properties:
        email:
          description: Email address to invite.
          type: string
        organisation:
          description: Organisation to invite into, as a resource name ("organisations/7")
            or a display name.
          type: string
        role:
          description: Role to grant, e.g. "owner", "admin", "member", or "viewer".
          type: string
      type: object
info:
  title: OpenAPI Spec for Solo APIs.
  version: v1
openapi: 3.0.1
paths: null
