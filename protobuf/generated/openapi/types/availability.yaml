components:
  schemas:
    freebusy.availability.v1.AvailabilityMatch:
      description: One resource matched by SearchAvailability, with a lead price for
        the period. Detailed slots/nights are fetched per resource via ComputeAvailability.
      properties:
        bookable:
          description: Whether the resource is bookable for the requested period and
            units.
          type: boolean
        displayName:
          description: Cached display name of the resource, for convenience.
          type: string
        mode:
          description: The resource's booking mode.
          enum:
          - BOOKING_MODE_UNSPECIFIED
          - BOOKING_MODE_TIME_SLOT
          - BOOKING_MODE_NIGHTLY
          type: string
        price:
          description: 'Lead price for the requested period: the stay total for NIGHTLY,
            or the slot price for TIME_SLOT. Used for sorting and display.'
          properties:
            currencyCode:
              description: The three-letter currency code defined in ISO 4217.
              type: string
            nanos:
              description: Number of nano (10^-9) units of the amount. The value must
                be between -999,999,999 and +999,999,999 inclusive. If `units` is
                positive, `nanos` must be positive or zero. If `units` is zero, `nanos`
                can be positive, zero, or negative. If `units` is negative, `nanos`
                must be negative or zero. For example $-1.75 is represented as `units`=-1
                and `nanos`=-750,000,000.
              format: int32
              type: integer
            units:
              description: The whole units of the amount. For example if `currencyCode`
                is `"USD"`, then 1 unit is one US dollar.
              format: int64
              type: integer
              x-kubernetes-int-or-string: true
          type: object
        resource:
          description: 'The matching resource. Format: resources/{resource}'
          type: string
      type: object
    freebusy.availability.v1.BatchComputeAvailabilityRequest:
      description: Request message for BatchComputeAvailability. Each entry is a full
        ComputeAvailabilityRequest (AIP-231), so per-resource duration, offering,
        and units all work in batch exactly as they do in the single call.
      properties:
        requests:
          description: The individual compute requests. Results are returned in the
            same order.
          items:
            description: Request message for ComputeAvailability.
            properties:
              dateRange:
                description: A calendar-date range in the resource's timezone, the
                  natural form for NIGHTLY resources; end_date is the check-out date.
                properties:
                  endDate:
                    description: Exclusive end date of the range.
                    properties:
                      day:
                        description: Day of a month. Must be from 1 to 31 and valid
                          for the year and month, or 0 to specify a year by itself
                          or a year and month where the day isn't significant.
                        format: int32
                        type: integer
                      month:
                        description: Month of a year. Must be from 1 to 12, or 0 to
                          specify a year without a month and day.
                        format: int32
                        type: integer
                      year:
                        description: Year of the date. Must be from 1 to 9999, or
                          0 to specify a date without a year.
                        format: int32
                        type: integer
                    type: object
                  startDate:
                    description: Inclusive first date of the range.
                    properties:
                      day:
                        description: Day of a month. Must be from 1 to 31 and valid
                          for the year and month, or 0 to specify a year by itself
                          or a year and month where the day isn't significant.
                        format: int32
                        type: integer
                      month:
                        description: Month of a year. Must be from 1 to 12, or 0 to
                          specify a year without a month and day.
                        format: int32
                        type: integer
                      year:
                        description: Year of the date. Must be from 1 to 9999, or
                          0 to specify a date without a year.
                        format: int32
                        type: integer
                    type: object
                type: object
              duration:
                description: Slot length for TIME_SLOT resources. Ignored when offering
                  is set or for NIGHTLY resources.
                type: string
              offering:
                description: 'Offering to derive duration and price from. Takes precedence
                  over duration. Format: resources/{resource}/offerings/{offering}'
                type: string
              resource:
                description: 'The resource to compute availability for. Format: resources/{resource}'
                type: string
              units:
                description: Number of units required to be free. Defaults to 1.
                format: int32
                type: integer
              window:
                description: An exact time window, the natural form for TIME_SLOT
                  resources.
                properties:
                  endTime:
                    description: Exclusive end of the interval.
                    format: date-time
                    type: string
                  startTime:
                    description: Inclusive start of the interval.
                    format: date-time
                    type: string
                type: object
            type: object
          type: array
      type: object
    freebusy.availability.v1.BatchComputeAvailabilityResponse:
      description: Response message for BatchComputeAvailability.
      properties:
        resources:
          description: Availability per request, in request order.
          items:
            description: Availability for one resource, used in batch responses.
            properties:
              mode:
                description: Which shape is populated, matching the resource's booking_mode.
                enum:
                - BOOKING_MODE_UNSPECIFIED
                - BOOKING_MODE_TIME_SLOT
                - BOOKING_MODE_NIGHTLY
                type: string
              nights:
                description: Per-night availability, when mode is NIGHTLY.
                items:
                  description: Per-night availability, produced for NIGHTLY resources.
                  properties:
                    closed:
                      description: Whether the resource is closed that night (exception/blackout).
                      type: boolean
                    freeUnits:
                      description: Number of units of the pool free that night.
                      format: int32
                      type: integer
                    night:
                      description: The night, in the resource's local timezone.
                      properties:
                        day:
                          description: Day of a month. Must be from 1 to 31 and valid
                            for the year and month, or 0 to specify a year by itself
                            or a year and month where the day isn't significant.
                          format: int32
                          type: integer
                        month:
                          description: Month of a year. Must be from 1 to 12, or 0
                            to specify a year without a month and day.
                          format: int32
                          type: integer
                        year:
                          description: Year of the date. Must be from 1 to 9999, or
                            0 to specify a date without a year.
                          format: int32
                          type: integer
                      type: object
                    price:
                      description: Nightly price, when an offering was supplied.
                      properties:
                        currencyCode:
                          description: The three-letter currency code defined in ISO
                            4217.
                          type: string
                        nanos:
                          description: Number of nano (10^-9) units of the amount.
                            The value must be between -999,999,999 and +999,999,999
                            inclusive. If `units` is positive, `nanos` must be positive
                            or zero. If `units` is zero, `nanos` can be positive,
                            zero, or negative. If `units` is negative, `nanos` must
                            be negative or zero. For example $-1.75 is represented
                            as `units`=-1 and `nanos`=-750,000,000.
                          format: int32
                          type: integer
                        units:
                          description: The whole units of the amount. For example
                            if `currencyCode` is `"USD"`, then 1 unit is one US dollar.
                          format: int64
                          type: integer
                          x-kubernetes-int-or-string: true
                      type: object
                  type: object
                type: array
              resource:
                description: 'The resource these results are for. Format: resources/{resource}'
                type: string
              slots:
                description: Slots, when mode is TIME_SLOT.
                items:
                  description: A discrete bookable time slot, produced for TIME_SLOT
                    resources.
                  properties:
                    bookable:
                      description: Whether the slot can actually be booked (free and
                        passes policy).
                      type: boolean
                    endTime:
                      description: Exclusive end of the slot (start + offering/requested
                        duration).
                      format: date-time
                      type: string
                    freeCount:
                      description: Number of units free in this slot (capacity minus
                        overlapping bookings).
                      format: int32
                      type: integer
                    price:
                      description: Price for booking this slot, when an offering was
                        supplied.
                      properties:
                        currencyCode:
                          description: The three-letter currency code defined in ISO
                            4217.
                          type: string
                        nanos:
                          description: Number of nano (10^-9) units of the amount.
                            The value must be between -999,999,999 and +999,999,999
                            inclusive. If `units` is positive, `nanos` must be positive
                            or zero. If `units` is zero, `nanos` can be positive,
                            zero, or negative. If `units` is negative, `nanos` must
                            be negative or zero. For example $-1.75 is represented
                            as `units`=-1 and `nanos`=-750,000,000.
                          format: int32
                          type: integer
                        units:
                          description: The whole units of the amount. For example
                            if `currencyCode` is `"USD"`, then 1 unit is one US dollar.
                          format: int64
                          type: integer
                          x-kubernetes-int-or-string: true
                      type: object
                    startTime:
                      description: Inclusive start of the slot.
                      format: date-time
                      type: string
                  type: object
                type: array
            type: object
          type: array
      type: object
    freebusy.availability.v1.BookableRange:
      description: A contiguous span that is bookable end to end.
      properties:
        bookable:
          description: Whether the whole span is bookable.
          type: boolean
        window:
          description: The bookable span.
          properties:
            endTime:
              description: Exclusive end of the interval.
              format: date-time
              type: string
            startTime:
              description: Inclusive start of the interval.
              format: date-time
              type: string
          type: object
      type: object
    freebusy.availability.v1.CheckAvailabilityRequest:
      description: Request message for CheckAvailability.
      properties:
        dateRange:
          description: A calendar-date range in the resource's timezone, the natural
            form for NIGHTLY stays; end_date is the check-out date.
          properties:
            endDate:
              description: Exclusive end date of the range.
              properties:
                day:
                  description: Day of a month. Must be from 1 to 31 and valid for
                    the year and month, or 0 to specify a year by itself or a year
                    and month where the day isn't significant.
                  format: int32
                  type: integer
                month:
                  description: Month of a year. Must be from 1 to 12, or 0 to specify
                    a year without a month and day.
                  format: int32
                  type: integer
                year:
                  description: Year of the date. Must be from 1 to 9999, or 0 to specify
                    a date without a year.
                  format: int32
                  type: integer
              type: object
            startDate:
              description: Inclusive first date of the range.
              properties:
                day:
                  description: Day of a month. Must be from 1 to 31 and valid for
                    the year and month, or 0 to specify a year by itself or a year
                    and month where the day isn't significant.
                  format: int32
                  type: integer
                month:
                  description: Month of a year. Must be from 1 to 12, or 0 to specify
                    a year without a month and day.
                  format: int32
                  type: integer
                year:
                  description: Year of the date. Must be from 1 to 9999, or 0 to specify
                    a date without a year.
                  format: int32
                  type: integer
              type: object
          type: object
        offering:
          description: 'Offering whose duration/rules apply, when relevant. Format:
            resources/{resource}/offerings/{offering}'
          type: string
        resource:
          description: 'The resource to test. Format: resources/{resource}'
          type: string
        units:
          description: Number of units required to be free. Defaults to 1.
          format: int32
          type: integer
        window:
          description: An exact time window, the natural form for TIME_SLOT resources.
          properties:
            endTime:
              description: Exclusive end of the interval.
              format: date-time
              type: string
            startTime:
              description: Inclusive start of the interval.
              format: date-time
              type: string
          type: object
      type: object
    freebusy.availability.v1.CheckAvailabilityResponse:
      description: Response message for CheckAvailability.
      properties:
        bookable:
          description: Whether the span is bookable.
          type: boolean
        freeCount:
          description: Free units across the span (the minimum over the span).
          format: int32
          type: integer
        reasons:
          description: Why the span is not bookable, when bookable is false.
          items:
            description: A reason a span is not bookable, as a machine-readable code
              plus a human-readable explanation. Clients should branch on code, never
              on message.
            properties:
              code:
                description: Why the span is not bookable.
                enum:
                - CODE_UNSPECIFIED
                - CODE_NO_CAPACITY
                - CODE_OUTSIDE_HOURS
                - CODE_CLOSED
                - CODE_MIN_NIGHTS
                - CODE_MAX_NIGHTS
                - CODE_CHECKIN_DAY
                - CODE_CHECKOUT_DAY
                - CODE_MIN_NOTICE
                - CODE_MAX_ADVANCE
                - CODE_BUFFER_CONFLICT
                - CODE_RESOURCE_ARCHIVED
                type: string
              message:
                description: Human-readable explanation suitable for display, not
                  for parsing.
                type: string
            type: object
          type: array
      type: object
    freebusy.availability.v1.ComputeAvailabilityRequest:
      description: Request message for ComputeAvailability.
      properties:
        dateRange:
          description: A calendar-date range in the resource's timezone, the natural
            form for NIGHTLY resources; end_date is the check-out date.
          properties:
            endDate:
              description: Exclusive end date of the range.
              properties:
                day:
                  description: Day of a month. Must be from 1 to 31 and valid for
                    the year and month, or 0 to specify a year by itself or a year
                    and month where the day isn't significant.
                  format: int32
                  type: integer
                month:
                  description: Month of a year. Must be from 1 to 12, or 0 to specify
                    a year without a month and day.
                  format: int32
                  type: integer
                year:
                  description: Year of the date. Must be from 1 to 9999, or 0 to specify
                    a date without a year.
                  format: int32
                  type: integer
              type: object
            startDate:
              description: Inclusive first date of the range.
              properties:
                day:
                  description: Day of a month. Must be from 1 to 31 and valid for
                    the year and month, or 0 to specify a year by itself or a year
                    and month where the day isn't significant.
                  format: int32
                  type: integer
                month:
                  description: Month of a year. Must be from 1 to 12, or 0 to specify
                    a year without a month and day.
                  format: int32
                  type: integer
                year:
                  description: Year of the date. Must be from 1 to 9999, or 0 to specify
                    a date without a year.
                  format: int32
                  type: integer
              type: object
          type: object
        duration:
          description: Slot length for TIME_SLOT resources. Ignored when offering
            is set or for NIGHTLY resources.
          type: string
        offering:
          description: 'Offering to derive duration and price from. Takes precedence
            over duration. Format: resources/{resource}/offerings/{offering}'
          type: string
        resource:
          description: 'The resource to compute availability for. Format: resources/{resource}'
          type: string
        units:
          description: Number of units required to be free. Defaults to 1.
          format: int32
          type: integer
        window:
          description: An exact time window, the natural form for TIME_SLOT resources.
          properties:
            endTime:
              description: Exclusive end of the interval.
              format: date-time
              type: string
            startTime:
              description: Inclusive start of the interval.
              format: date-time
              type: string
          type: object
      type: object
    freebusy.availability.v1.ComputeAvailabilityResponse:
      description: Response message for ComputeAvailability.
      properties:
        mode:
          description: Which shape is populated, matching the resource's booking_mode.
          enum:
          - BOOKING_MODE_UNSPECIFIED
          - BOOKING_MODE_TIME_SLOT
          - BOOKING_MODE_NIGHTLY
          type: string
        nights:
          description: Per-night availability, when mode is NIGHTLY.
          items:
            description: Per-night availability, produced for NIGHTLY resources.
            properties:
              closed:
                description: Whether the resource is closed that night (exception/blackout).
                type: boolean
              freeUnits:
                description: Number of units of the pool free that night.
                format: int32
                type: integer
              night:
                description: The night, in the resource's local timezone.
                properties:
                  day:
                    description: Day of a month. Must be from 1 to 31 and valid for
                      the year and month, or 0 to specify a year by itself or a year
                      and month where the day isn't significant.
                    format: int32
                    type: integer
                  month:
                    description: Month of a year. Must be from 1 to 12, or 0 to specify
                      a year without a month and day.
                    format: int32
                    type: integer
                  year:
                    description: Year of the date. Must be from 1 to 9999, or 0 to
                      specify a date without a year.
                    format: int32
                    type: integer
                type: object
              price:
                description: Nightly price, when an offering was supplied.
                properties:
                  currencyCode:
                    description: The three-letter currency code defined in ISO 4217.
                    type: string
                  nanos:
                    description: Number of nano (10^-9) units of the amount. The value
                      must be between -999,999,999 and +999,999,999 inclusive. If
                      `units` is positive, `nanos` must be positive or zero. If `units`
                      is zero, `nanos` can be positive, zero, or negative. If `units`
                      is negative, `nanos` must be negative or zero. For example $-1.75
                      is represented as `units`=-1 and `nanos`=-750,000,000.
                    format: int32
                    type: integer
                  units:
                    description: The whole units of the amount. For example if `currencyCode`
                      is `"USD"`, then 1 unit is one US dollar.
                    format: int64
                    type: integer
                    x-kubernetes-int-or-string: true
                type: object
            type: object
          type: array
        slots:
          description: Slots, when mode is TIME_SLOT.
          items:
            description: A discrete bookable time slot, produced for TIME_SLOT resources.
            properties:
              bookable:
                description: Whether the slot can actually be booked (free and passes
                  policy).
                type: boolean
              endTime:
                description: Exclusive end of the slot (start + offering/requested
                  duration).
                format: date-time
                type: string
              freeCount:
                description: Number of units free in this slot (capacity minus overlapping
                  bookings).
                format: int32
                type: integer
              price:
                description: Price for booking this slot, when an offering was supplied.
                properties:
                  currencyCode:
                    description: The three-letter currency code defined in ISO 4217.
                    type: string
                  nanos:
                    description: Number of nano (10^-9) units of the amount. The value
                      must be between -999,999,999 and +999,999,999 inclusive. If
                      `units` is positive, `nanos` must be positive or zero. If `units`
                      is zero, `nanos` can be positive, zero, or negative. If `units`
                      is negative, `nanos` must be negative or zero. For example $-1.75
                      is represented as `units`=-1 and `nanos`=-750,000,000.
                    format: int32
                    type: integer
                  units:
                    description: The whole units of the amount. For example if `currencyCode`
                      is `"USD"`, then 1 unit is one US dollar.
                    format: int64
                    type: integer
                    x-kubernetes-int-or-string: true
                type: object
              startTime:
                description: Inclusive start of the slot.
                format: date-time
                type: string
            type: object
          type: array
      type: object
    freebusy.availability.v1.ComputeBookableRangesRequest:
      description: Request message for ComputeBookableRanges.
      properties:
        dateRange:
          description: A calendar-date range in the resource's timezone, the natural
            form for NIGHTLY resources.
          properties:
            endDate:
              description: Exclusive end date of the range.
              properties:
                day:
                  description: Day of a month. Must be from 1 to 31 and valid for
                    the year and month, or 0 to specify a year by itself or a year
                    and month where the day isn't significant.
                  format: int32
                  type: integer
                month:
                  description: Month of a year. Must be from 1 to 12, or 0 to specify
                    a year without a month and day.
                  format: int32
                  type: integer
                year:
                  description: Year of the date. Must be from 1 to 9999, or 0 to specify
                    a date without a year.
                  format: int32
                  type: integer
              type: object
            startDate:
              description: Inclusive first date of the range.
              properties:
                day:
                  description: Day of a month. Must be from 1 to 31 and valid for
                    the year and month, or 0 to specify a year by itself or a year
                    and month where the day isn't significant.
                  format: int32
                  type: integer
                month:
                  description: Month of a year. Must be from 1 to 12, or 0 to specify
                    a year without a month and day.
                  format: int32
                  type: integer
                year:
                  description: Year of the date. Must be from 1 to 9999, or 0 to specify
                    a date without a year.
                  format: int32
                  type: integer
              type: object
          type: object
        duration:
          description: Minimum span length for TIME_SLOT resources.
          type: string
        offering:
          description: 'Offering to derive duration/rules from. Format: resources/{resource}/offerings/{offering}'
          type: string
        resource:
          description: 'The resource to compute bookable ranges for. Format: resources/{resource}'
          type: string
        units:
          description: Number of units required to be free. Defaults to 1.
          format: int32
          type: integer
        window:
          description: An exact time window, the natural form for TIME_SLOT resources.
          properties:
            endTime:
              description: Exclusive end of the interval.
              format: date-time
              type: string
            startTime:
              description: Inclusive start of the interval.
              format: date-time
              type: string
          type: object
      type: object
    freebusy.availability.v1.ComputeBookableRangesResponse:
      description: Response message for ComputeBookableRanges.
      properties:
        ranges:
          description: The bookable ranges within the window.
          items:
            description: A contiguous span that is bookable end to end.
            properties:
              bookable:
                description: Whether the whole span is bookable.
                type: boolean
              window:
                description: The bookable span.
                properties:
                  endTime:
                    description: Exclusive end of the interval.
                    format: date-time
                    type: string
                  startTime:
                    description: Inclusive start of the interval.
                    format: date-time
                    type: string
                type: object
            type: object
          type: array
      type: object
    freebusy.availability.v1.NightAvailability:
      description: Per-night availability, produced for NIGHTLY resources.
      properties:
        closed:
          description: Whether the resource is closed that night (exception/blackout).
          type: boolean
        freeUnits:
          description: Number of units of the pool free that night.
          format: int32
          type: integer
        night:
          description: The night, in the resource's local timezone.
          properties:
            day:
              description: Day of a month. Must be from 1 to 31 and valid for the
                year and month, or 0 to specify a year by itself or a year and month
                where the day isn't significant.
              format: int32
              type: integer
            month:
              description: Month of a year. Must be from 1 to 12, or 0 to specify
                a year without a month and day.
              format: int32
              type: integer
            year:
              description: Year of the date. Must be from 1 to 9999, or 0 to specify
                a date without a year.
              format: int32
              type: integer
          type: object
        price:
          description: Nightly price, when an offering was supplied.
          properties:
            currencyCode:
              description: The three-letter currency code defined in ISO 4217.
              type: string
            nanos:
              description: Number of nano (10^-9) units of the amount. The value must
                be between -999,999,999 and +999,999,999 inclusive. If `units` is
                positive, `nanos` must be positive or zero. If `units` is zero, `nanos`
                can be positive, zero, or negative. If `units` is negative, `nanos`
                must be negative or zero. For example $-1.75 is represented as `units`=-1
                and `nanos`=-750,000,000.
              format: int32
              type: integer
            units:
              description: The whole units of the amount. For example if `currencyCode`
                is `"USD"`, then 1 unit is one US dollar.
              format: int64
              type: integer
              x-kubernetes-int-or-string: true
          type: object
      type: object
    freebusy.availability.v1.ResourceAvailability:
      description: Availability for one resource, used in batch responses.
      properties:
        mode:
          description: Which shape is populated, matching the resource's booking_mode.
          enum:
          - BOOKING_MODE_UNSPECIFIED
          - BOOKING_MODE_TIME_SLOT
          - BOOKING_MODE_NIGHTLY
          type: string
        nights:
          description: Per-night availability, when mode is NIGHTLY.
          items:
            description: Per-night availability, produced for NIGHTLY resources.
            properties:
              closed:
                description: Whether the resource is closed that night (exception/blackout).
                type: boolean
              freeUnits:
                description: Number of units of the pool free that night.
                format: int32
                type: integer
              night:
                description: The night, in the resource's local timezone.
                properties:
                  day:
                    description: Day of a month. Must be from 1 to 31 and valid for
                      the year and month, or 0 to specify a year by itself or a year
                      and month where the day isn't significant.
                    format: int32
                    type: integer
                  month:
                    description: Month of a year. Must be from 1 to 12, or 0 to specify
                      a year without a month and day.
                    format: int32
                    type: integer
                  year:
                    description: Year of the date. Must be from 1 to 9999, or 0 to
                      specify a date without a year.
                    format: int32
                    type: integer
                type: object
              price:
                description: Nightly price, when an offering was supplied.
                properties:
                  currencyCode:
                    description: The three-letter currency code defined in ISO 4217.
                    type: string
                  nanos:
                    description: Number of nano (10^-9) units of the amount. The value
                      must be between -999,999,999 and +999,999,999 inclusive. If
                      `units` is positive, `nanos` must be positive or zero. If `units`
                      is zero, `nanos` can be positive, zero, or negative. If `units`
                      is negative, `nanos` must be negative or zero. For example $-1.75
                      is represented as `units`=-1 and `nanos`=-750,000,000.
                    format: int32
                    type: integer
                  units:
                    description: The whole units of the amount. For example if `currencyCode`
                      is `"USD"`, then 1 unit is one US dollar.
                    format: int64
                    type: integer
                    x-kubernetes-int-or-string: true
                type: object
            type: object
          type: array
        resource:
          description: 'The resource these results are for. Format: resources/{resource}'
          type: string
        slots:
          description: Slots, when mode is TIME_SLOT.
          items:
            description: A discrete bookable time slot, produced for TIME_SLOT resources.
            properties:
              bookable:
                description: Whether the slot can actually be booked (free and passes
                  policy).
                type: boolean
              endTime:
                description: Exclusive end of the slot (start + offering/requested
                  duration).
                format: date-time
                type: string
              freeCount:
                description: Number of units free in this slot (capacity minus overlapping
                  bookings).
                format: int32
                type: integer
              price:
                description: Price for booking this slot, when an offering was supplied.
                properties:
                  currencyCode:
                    description: The three-letter currency code defined in ISO 4217.
                    type: string
                  nanos:
                    description: Number of nano (10^-9) units of the amount. The value
                      must be between -999,999,999 and +999,999,999 inclusive. If
                      `units` is positive, `nanos` must be positive or zero. If `units`
                      is zero, `nanos` can be positive, zero, or negative. If `units`
                      is negative, `nanos` must be negative or zero. For example $-1.75
                      is represented as `units`=-1 and `nanos`=-750,000,000.
                    format: int32
                    type: integer
                  units:
                    description: The whole units of the amount. For example if `currencyCode`
                      is `"USD"`, then 1 unit is one US dollar.
                    format: int64
                    type: integer
                    x-kubernetes-int-or-string: true
                type: object
              startTime:
                description: Inclusive start of the slot.
                format: date-time
                type: string
            type: object
          type: array
      type: object
    freebusy.availability.v1.SearchAvailabilityRequest:
      description: 'Request message for SearchAvailability. Sweeps the catalog for
        resources that are bookable over a period for a given party size, narrowed
        by a resource filter and sorted for presentation. This is the storefront query:
        one call returns the matching resources with a lead price, rather than the
        caller listing resources and computing availability for each.'
      properties:
        dateRange:
          description: A calendar-date range in each resource's timezone, the natural
            form for NIGHTLY resources; end_date is the check-out date.
          properties:
            endDate:
              description: Exclusive end date of the range.
              properties:
                day:
                  description: Day of a month. Must be from 1 to 31 and valid for
                    the year and month, or 0 to specify a year by itself or a year
                    and month where the day isn't significant.
                  format: int32
                  type: integer
                month:
                  description: Month of a year. Must be from 1 to 12, or 0 to specify
                    a year without a month and day.
                  format: int32
                  type: integer
                year:
                  description: Year of the date. Must be from 1 to 9999, or 0 to specify
                    a date without a year.
                  format: int32
                  type: integer
              type: object
            startDate:
              description: Inclusive first date of the range.
              properties:
                day:
                  description: Day of a month. Must be from 1 to 31 and valid for
                    the year and month, or 0 to specify a year by itself or a year
                    and month where the day isn't significant.
                  format: int32
                  type: integer
                month:
                  description: Month of a year. Must be from 1 to 12, or 0 to specify
                    a year without a month and day.
                  format: int32
                  type: integer
                year:
                  description: Year of the date. Must be from 1 to 9999, or 0 to specify
                    a date without a year.
                  format: int32
                  type: integer
              type: object
          type: object
        filter:
          description: Filter (AIP-160) over resource fields to narrow the catalog,
            e.g. `type = RESOURCE_TYPE_ROOM`, `tags:"beachfront"`, or a display_name
            match.
          type: string
        includeUnavailable:
          description: If true, include resources that matched the filter but are
            not bookable for the period (with bookable=false), instead of dropping
            them.
          type: boolean
        orderBy:
          description: Sort order for matches, e.g. "price" or "price desc". Defaults
            to price ascending.
          type: string
        pageSize:
          description: Maximum number of matches to return. The server may cap this.
          format: int32
          type: integer
        pageToken:
          description: Page token from a previous SearchAvailability call's next_page_token.
          type: string
        units:
          description: Number of units / party size required free. Defaults to 1.
          format: int32
          type: integer
        window:
          description: An exact time window, the natural form for TIME_SLOT resources.
          properties:
            endTime:
              description: Exclusive end of the interval.
              format: date-time
              type: string
            startTime:
              description: Inclusive start of the interval.
              format: date-time
              type: string
          type: object
      type: object
    freebusy.availability.v1.SearchAvailabilityResponse:
      description: Response message for SearchAvailability.
      properties:
        matches:
          description: The matching resources, ordered per order_by.
          items:
            description: One resource matched by SearchAvailability, with a lead price
              for the period. Detailed slots/nights are fetched per resource via ComputeAvailability.
            properties:
              bookable:
                description: Whether the resource is bookable for the requested period
                  and units.
                type: boolean
              displayName:
                description: Cached display name of the resource, for convenience.
                type: string
              mode:
                description: The resource's booking mode.
                enum:
                - BOOKING_MODE_UNSPECIFIED
                - BOOKING_MODE_TIME_SLOT
                - BOOKING_MODE_NIGHTLY
                type: string
              price:
                description: 'Lead price for the requested period: the stay total
                  for NIGHTLY, or the slot price for TIME_SLOT. Used for sorting and
                  display.'
                properties:
                  currencyCode:
                    description: The three-letter currency code defined in ISO 4217.
                    type: string
                  nanos:
                    description: Number of nano (10^-9) units of the amount. The value
                      must be between -999,999,999 and +999,999,999 inclusive. If
                      `units` is positive, `nanos` must be positive or zero. If `units`
                      is zero, `nanos` can be positive, zero, or negative. If `units`
                      is negative, `nanos` must be negative or zero. For example $-1.75
                      is represented as `units`=-1 and `nanos`=-750,000,000.
                    format: int32
                    type: integer
                  units:
                    description: The whole units of the amount. For example if `currencyCode`
                      is `"USD"`, then 1 unit is one US dollar.
                    format: int64
                    type: integer
                    x-kubernetes-int-or-string: true
                type: object
              resource:
                description: 'The matching resource. Format: resources/{resource}'
                type: string
            type: object
          type: array
        nextPageToken:
          description: Token to pass as page_token to retrieve the next page; empty
            when no more.
          type: string
      type: object
    freebusy.availability.v1.Slot:
      description: A discrete bookable time slot, produced for TIME_SLOT resources.
      properties:
        bookable:
          description: Whether the slot can actually be booked (free and passes policy).
          type: boolean
        endTime:
          description: Exclusive end of the slot (start + offering/requested duration).
          format: date-time
          type: string
        freeCount:
          description: Number of units free in this slot (capacity minus overlapping
            bookings).
          format: int32
          type: integer
        price:
          description: Price for booking this slot, when an offering was supplied.
          properties:
            currencyCode:
              description: The three-letter currency code defined in ISO 4217.
              type: string
            nanos:
              description: Number of nano (10^-9) units of the amount. The value must
                be between -999,999,999 and +999,999,999 inclusive. If `units` is
                positive, `nanos` must be positive or zero. If `units` is zero, `nanos`
                can be positive, zero, or negative. If `units` is negative, `nanos`
                must be negative or zero. For example $-1.75 is represented as `units`=-1
                and `nanos`=-750,000,000.
              format: int32
              type: integer
            units:
              description: The whole units of the amount. For example if `currencyCode`
                is `"USD"`, then 1 unit is one US dollar.
              format: int64
              type: integer
              x-kubernetes-int-or-string: true
          type: object
        startTime:
          description: Inclusive start of the slot.
          format: date-time
          type: string
      type: object
    freebusy.availability.v1.UnbookableReason:
      description: A reason a span is not bookable, as a machine-readable code plus
        a human-readable explanation. Clients should branch on code, never on message.
      properties:
        code:
          description: Why the span is not bookable.
          enum:
          - CODE_UNSPECIFIED
          - CODE_NO_CAPACITY
          - CODE_OUTSIDE_HOURS
          - CODE_CLOSED
          - CODE_MIN_NIGHTS
          - CODE_MAX_NIGHTS
          - CODE_CHECKIN_DAY
          - CODE_CHECKOUT_DAY
          - CODE_MIN_NOTICE
          - CODE_MAX_ADVANCE
          - CODE_BUFFER_CONFLICT
          - CODE_RESOURCE_ARCHIVED
          type: string
        message:
          description: Human-readable explanation suitable for display, not for parsing.
          type: string
      type: object
info:
  title: OpenAPI Spec for Solo APIs.
  version: v1
openapi: 3.0.1
paths: null
