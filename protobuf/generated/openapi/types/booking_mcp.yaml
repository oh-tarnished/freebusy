components:
  schemas:
    freebusy.booking.v1.BookSlotArgs:
      description: Arguments for the "book_slot" prompt.
      properties:
        promoCode:
          description: Promo code to apply, if any.
          type: string
        startTime:
          description: Start of the booking (RFC 3339, e.g. "2026-07-01T14:00:00Z").
          format: date-time
          type: string
        unit:
          description: Unit to book, as a resource name ("properties/9/units/42")
            or a display name.
          type: string
        units:
          description: Number of units / party size. Defaults to 1.
          format: int32
          type: integer
      type: object
info:
  title: OpenAPI Spec for Solo APIs.
  version: v1
openapi: 3.0.1
paths: null
