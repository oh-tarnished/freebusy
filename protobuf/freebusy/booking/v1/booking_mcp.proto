syntax = "proto3";

package freebusy.booking.v1;

import "google/api/field_behavior.proto";
import "google/protobuf/timestamp.proto";

option go_package = "github.com/oh-tarnished/freebusy/protobuf/generated/go/booking/v1/bookingpbv1;bookingpbv1";
option java_multiple_files = true;
option java_outer_classname = "BookingMcpProto";
option java_package = "dev.ohtarnished.freebusy.booking.v1";

// Arguments for the "book_slot" prompt.
message BookSlotArgs {
  // Field number of the removed `offering` arg; never reuse it (a unit now
  // carries its own pricing).
  reserved 2;

  // Unit to book, as a resource name ("properties/9/units/42") or a display name.
  string unit = 1 [(google.api.field_behavior) = REQUIRED];

  // Start of the booking (RFC 3339, e.g. "2026-07-01T14:00:00Z").
  google.protobuf.Timestamp start_time = 3 [(google.api.field_behavior) = REQUIRED];

  // Number of units / party size. Defaults to 1.
  int32 units = 4 [(google.api.field_behavior) = OPTIONAL];

  // Promo code to apply, if any.
  string promo_code = 5 [(google.api.field_behavior) = OPTIONAL];
}
