# @generated by tools/protobuf/envoy — DO NOT EDIT
# Re-generate with `just gen envoy` (or `just gen`).
# Source: protobuf/generated/openapi/**/*_service.openapi.yaml
#
# Envoy proxy: browser (grpc-web / HTTP 1.1) → freebusy gRPC backend (HTTP/2).
# All freebusy services live on one gRPC backend, so every route targets the
# single freebusy_backend cluster.
# Listener: 0.0.0.0:8080
# Admin UI:  0.0.0.0:9901

admin:
  address:
    socket_address:
      address: 0.0.0.0
      port_value: 9901

static_resources:

  listeners:
    - name: grpc_web_listener
      address:
        socket_address:
          address: 0.0.0.0
          port_value: 8080
      filter_chains:
        - filters:
            - name: envoy.filters.network.http_connection_manager
              typed_config:
                "@type": type.googleapis.com/envoy.extensions.filters.network.http_connection_manager.v3.HttpConnectionManager
                codec_type: AUTO
                stat_prefix: grpc_web
                route_config:
                  name: local_route
                  virtual_hosts:
                    - name: freebusy_services
                      domains: ["*"]
                      routes:

                        - match:
                            prefix: "/freebusy.availability.v1.AvailabilityService/"
                          route:
                            cluster: freebusy_backend
                            timeout: 60s

                        - match:
                            prefix: "/freebusy.booking.v1.BookingService/"
                          route:
                            cluster: freebusy_backend
                            timeout: 60s

                        - match:
                            prefix: "/freebusy.channel.v1.ChannelService/"
                          route:
                            cluster: freebusy_backend
                            timeout: 60s

                        - match:
                            prefix: "/freebusy.identity.v1.IdentityService/"
                          route:
                            cluster: freebusy_backend
                            timeout: 60s

                        - match:
                            prefix: "/freebusy.organisation.v1.OrganisationService/"
                          route:
                            cluster: freebusy_backend
                            timeout: 60s

                        - match:
                            prefix: "/freebusy.promocode.v1.PromoCodeService/"
                          route:
                            cluster: freebusy_backend
                            timeout: 60s

                        - match:
                            prefix: "/freebusy.property.v1.LicenceService/"
                          route:
                            cluster: freebusy_backend
                            timeout: 60s

                        - match:
                            prefix: "/freebusy.property.v1.PropertyService/"
                          route:
                            cluster: freebusy_backend
                            timeout: 60s

                        - match:
                            prefix: "/freebusy.resource.v1.ResourceService/"
                          route:
                            cluster: freebusy_backend
                            timeout: 60s

                        - match:
                            prefix: "/freebusy.schedule.v1.ScheduleService/"
                          route:
                            cluster: freebusy_backend
                            timeout: 60s

                      cors:
                        allow_origin_string_match:
                          - prefix: "*"
                        allow_methods: GET, PUT, DELETE, POST, OPTIONS
                        allow_headers: >-
                          keep-alive,user-agent,cache-control,content-type,content-transfer-encoding,
                          x-accept-content-transfer-encoding,x-accept-response-streaming,
                          x-user-agent,x-grpc-web,grpc-timeout,authorization
                        expose_headers: grpc-status,grpc-message,grpc-status-details-bin
                        max_age: "1728000"
                http_filters:
                  - name: envoy.filters.http.grpc_web
                    typed_config:
                      "@type": type.googleapis.com/envoy.extensions.filters.http.grpc_web.v3.GrpcWeb
                  - name: envoy.filters.http.cors
                    typed_config:
                      "@type": type.googleapis.com/envoy.extensions.filters.http.cors.v3.CorsPolicy
                  - name: envoy.filters.http.router
                    typed_config:
                      "@type": type.googleapis.com/envoy.extensions.filters.http.router.v3.Router

  clusters:

    - name: freebusy_backend
      connect_timeout: 5s
      type: STRICT_DNS
      # STRICT_DNS defaults to AUTO, which prefers AAAA. Inside Docker that
      # resolves the backend host to a non-routable IPv6 and every request dies
      # with "Network is unreachable". Pin the family.
      dns_lookup_family: V4_ONLY
      http2_protocol_options: {}
      lb_policy: ROUND_ROBIN
      # The freebusy gRPC port terminates TLS, so a plaintext upstream is an
      # instant 503 (UF/UPE). The SNI must match a SAN on the backend cert —
      # the mkcert dev cert carries `freebusy-dev`, not `host.docker.internal` —
      # so the dialled host and the presented server name deliberately differ.
      transport_socket:
        name: envoy.transport_sockets.tls
        typed_config:
          "@type": type.googleapis.com/envoy.extensions.transport_sockets.tls.v3.UpstreamTlsContext
          sni: freebusy-dev
          common_tls_context:
            alpn_protocols: ["h2"]
            # Envoy's upstream client defaults to a TLS 1.2 ceiling. The Go
            # server negotiates 1.3, so without raising the max it aborts the
            # handshake with TLSV1_ALERT_PROTOCOL_VERSION.
            tls_params:
              tls_minimum_protocol_version: TLSv1_2
              tls_maximum_protocol_version: TLSv1_3
      load_assignment:
        cluster_name: freebusy_backend
        endpoints:
          - lb_endpoints:
              - endpoint:
                  address:
                    socket_address:
                      address: host.docker.internal
                      port_value: 50051
