// Code generated by protoc-gen-orm. DO NOT EDIT.
// versions:
// 	protoc-gen-orm 1.4.2
// 	protoc (unknown)
// source: freebusy/shared/v1/idempotency.proto, freebusy/shared/v1/types.proto
//
// database: freebusy
// schema:   shared
//
// Proto-facing repository interfaces; adapters compose the generated gorm and GraphQL outputs.
//
// orm — https://github.com/the-protobuf-project/orm

package shared

import (
	"context"
	"github.com/oh-tarnished/freebusy/internal/database/gorm/filterx"
	"github.com/oh-tarnished/freebusy/internal/database/hasura/freebusyql"
	"github.com/oh-tarnished/freebusy/internal/database/repository/repox"
	"github.com/oh-tarnished/freebusy/protobuf/generated/go/shared/v1/sharedpbv1"
	"gorm.io/gorm"
)

// IdempotencyKeyRepository is the provider-agnostic persistence surface for
// IdempotencyKey (idempotencyKeys/{idempotency_key}): full-message writes, AIP-160/AIP-132 lists with
// opaque page tokens, field-mask updates, and etag optimistic concurrency.
type IdempotencyKeyRepository interface {
	// Create persists m and returns the stored record.
	Create(ctx context.Context, m *sharedpbv1.IdempotencyKey) (*sharedpbv1.IdempotencyKey, error)
	// Get returns the record addressed by its resource name, or
	// repox.ErrNotFound.
	Get(ctx context.Context, name string) (*sharedpbv1.IdempotencyKey, error)
	// List returns one page of records.
	List(ctx context.Context, in repox.ListInput) ([]*sharedpbv1.IdempotencyKey, string, error)
	// Update persists the masked fields of m; an empty mask replaces every
	// mutable field. m.Etag, when set, guards against concurrent writes
	// (repox.ErrConflict).
	Update(ctx context.Context, m *sharedpbv1.IdempotencyKey, paths []string) (*sharedpbv1.IdempotencyKey, error)
	// Delete removes the record addressed by its resource name.
	Delete(ctx context.Context, name string) error
}

// IdempotencyKeyHooks lets custom logic run inside the generated IdempotencyKey
// adapters without editing them: normalization/derivation before writes,
// derived fields after reads, and guards before deletes. Nil funcs are skipped.
type IdempotencyKeyHooks struct {
	// BeforeCreate runs after name/id resolution, before the row is written.
	BeforeCreate func(ctx context.Context, m *sharedpbv1.IdempotencyKey) error
	// AfterRead runs on every record a read returns (Get, List, and the
	// re-reads writes return).
	AfterRead func(ctx context.Context, m *sharedpbv1.IdempotencyKey) error
	// BeforeUpdate runs after the mask is applied to the merged record,
	// before it is written.
	BeforeUpdate func(ctx context.Context, existing, merged *sharedpbv1.IdempotencyKey, paths []string) error
	// BeforeDelete runs before the row is removed; returning an error vetoes.
	BeforeDelete func(ctx context.Context, name string) error
}

// Repositories bundles this schema's repository surfaces behind one factory.
type Repositories struct {
	IdempotencyKeys IdempotencyKeyRepository
}

// options collects the per-resource customizations the factories thread into
// the adapters they build.
type options struct {
	idempotencyKeyHooks        IdempotencyKeyHooks
	idempotencyKeyOverrides    map[string]filterx.SQLHandler
	idempotencyKeyGQLOverrides map[string]filterx.GraphQLHandler
}

// Option customizes the adapters a factory builds.
type Option func(*options)

// WithIdempotencyKeyHooks installs h on the IdempotencyKey adapters.
func WithIdempotencyKeyHooks(h IdempotencyKeyHooks) Option {
	return func(o *options) { o.idempotencyKeyHooks = h }
}

// WithIdempotencyKeyListOverride substitutes the generated filter dispatch for one
// IdempotencyKey filter field (e.g. a derived state computed via subqueries).
func WithIdempotencyKeyListOverride(field string, h filterx.SQLHandler) Option {
	return func(o *options) {
		if o.idempotencyKeyOverrides == nil {
			o.idempotencyKeyOverrides = map[string]filterx.SQLHandler{}
		}
		o.idempotencyKeyOverrides[field] = h
	}
}

// WithIdempotencyKeyGraphQLListOverride substitutes the generated filter dispatch
// for one IdempotencyKey filter field on the GraphQL adapters.
func WithIdempotencyKeyGraphQLListOverride(field string, h filterx.GraphQLHandler) Option {
	return func(o *options) {
		if o.idempotencyKeyGQLOverrides == nil {
			o.idempotencyKeyGQLOverrides = map[string]filterx.GraphQLHandler{}
		}
		o.idempotencyKeyGQLOverrides[field] = h
	}
}

// New picks the adapter set for the live handle in conn: GraphQL when a
// client connection is present, otherwise GORM.
func New(conn repox.Conn, opts ...Option) Repositories {
	if conn.GraphQL != nil {
		return NewGraphQL(conn.GraphQL, opts...)
	}
	return NewGorm(conn.Gorm, opts...)
}

// NewGraphQL builds the GraphQL adapter set over svc.
func NewGraphQL(svc *freebusyql.Service, opts ...Option) Repositories {
	var o options
	for _, opt := range opts {
		opt(&o)
	}
	return Repositories{
		IdempotencyKeys: &GraphQLIdempotencyKeyRepository{Svc: svc, Hooks: o.idempotencyKeyHooks, ListOverrides: o.idempotencyKeyGQLOverrides},
	}
}

// NewGorm builds the GORM adapter set over db.
func NewGorm(db *gorm.DB, opts ...Option) Repositories {
	var o options
	for _, opt := range opts {
		opt(&o)
	}
	return Repositories{
		IdempotencyKeys: &GormIdempotencyKeyRepository{DB: db, Hooks: o.idempotencyKeyHooks, ListOverrides: o.idempotencyKeyOverrides},
	}
}
