// Code generated by protoc-gen-orm. DO NOT EDIT.
// versions:
// 	protoc-gen-orm v1.4.1+dirty
// 	protoc (unknown)
// source: freebusy/channel/v1/channel.proto, freebusy/channel/v1/channel_messages.proto
//
// database: freebusy
// schema:   channel
//
// Proto-facing repository interfaces; adapters compose the generated gorm and GraphQL outputs.
//
// orm — https://github.com/the-protobuf-project/orm

package channel

import (
	"context"
	"github.com/oh-tarnished/freebusy/internal/database/gorm/filterx"
	"github.com/oh-tarnished/freebusy/internal/database/hasura/freebusyql"
	"github.com/oh-tarnished/freebusy/internal/database/repository/repox"
	"github.com/oh-tarnished/freebusy/protobuf/generated/go/channel/v1/channelpbv1"
	"gorm.io/gorm"
)

// ChannelRepository is the provider-agnostic persistence surface for
// Channel (channels/{channel}): full-message writes, AIP-160/AIP-132 lists with
// opaque page tokens, field-mask updates, and etag optimistic concurrency.
type ChannelRepository interface {
	// Create persists m and returns the stored record.
	Create(ctx context.Context, m *channelpbv1.Channel) (*channelpbv1.Channel, error)
	// Get returns the record addressed by its resource name, or
	// repox.ErrNotFound.
	Get(ctx context.Context, name string) (*channelpbv1.Channel, error)
	// List returns one page of records.
	List(ctx context.Context, in repox.ListInput) ([]*channelpbv1.Channel, string, error)
	// Update persists the masked fields of m; an empty mask replaces every
	// mutable field. m.Etag, when set, guards against concurrent writes
	// (repox.ErrConflict).
	Update(ctx context.Context, m *channelpbv1.Channel, paths []string) (*channelpbv1.Channel, error)
	// Delete removes the record addressed by its resource name.
	Delete(ctx context.Context, name string) error
}

// ChannelHooks lets custom logic run inside the generated Channel
// adapters without editing them: normalization/derivation before writes,
// derived fields after reads, and guards before deletes. Nil funcs are skipped.
type ChannelHooks struct {
	// BeforeCreate runs after name/id resolution, before the row is written.
	BeforeCreate func(ctx context.Context, m *channelpbv1.Channel) error
	// AfterRead runs on every record a read returns (Get, List, and the
	// re-reads writes return).
	AfterRead func(ctx context.Context, m *channelpbv1.Channel) error
	// BeforeUpdate runs after the mask is applied to the merged record,
	// before it is written.
	BeforeUpdate func(ctx context.Context, existing, merged *channelpbv1.Channel, paths []string) error
	// BeforeDelete runs before the row is removed; returning an error vetoes.
	BeforeDelete func(ctx context.Context, name string) error
}

// UnitMappingRepository is the provider-agnostic persistence surface for
// UnitMapping (channels/{channel}/unitMappings/{unit_mapping}): full-message writes, AIP-160/AIP-132 lists with
// opaque page tokens, field-mask updates, and etag optimistic concurrency.
type UnitMappingRepository interface {
	// Create persists m under parent and returns the stored record.
	Create(ctx context.Context, parent string, m *channelpbv1.UnitMapping) (*channelpbv1.UnitMapping, error)
	// Get returns the record addressed by its resource name, or
	// repox.ErrNotFound.
	Get(ctx context.Context, name string) (*channelpbv1.UnitMapping, error)
	// List returns one page of records under parent.
	List(ctx context.Context, parent string, in repox.ListInput) ([]*channelpbv1.UnitMapping, string, error)
	// Update persists the masked fields of m; an empty mask replaces every
	// mutable field. m.Etag, when set, guards against concurrent writes
	// (repox.ErrConflict).
	Update(ctx context.Context, m *channelpbv1.UnitMapping, paths []string) (*channelpbv1.UnitMapping, error)
	// Delete removes the record addressed by its resource name.
	Delete(ctx context.Context, name string) error
}

// UnitMappingHooks lets custom logic run inside the generated UnitMapping
// adapters without editing them: normalization/derivation before writes,
// derived fields after reads, and guards before deletes. Nil funcs are skipped.
type UnitMappingHooks struct {
	// BeforeCreate runs after name/id resolution, before the row is written.
	BeforeCreate func(ctx context.Context, m *channelpbv1.UnitMapping) error
	// AfterRead runs on every record a read returns (Get, List, and the
	// re-reads writes return).
	AfterRead func(ctx context.Context, m *channelpbv1.UnitMapping) error
	// BeforeUpdate runs after the mask is applied to the merged record,
	// before it is written.
	BeforeUpdate func(ctx context.Context, existing, merged *channelpbv1.UnitMapping, paths []string) error
	// BeforeDelete runs before the row is removed; returning an error vetoes.
	BeforeDelete func(ctx context.Context, name string) error
}

// Repositories bundles this schema's repository surfaces behind one factory.
type Repositories struct {
	Channels     ChannelRepository
	UnitMappings UnitMappingRepository
}

// options collects the per-resource customizations the factories thread into
// the adapters they build.
type options struct {
	channelHooks            ChannelHooks
	channelOverrides        map[string]filterx.SQLHandler
	unitMappingHooks        UnitMappingHooks
	unitMappingOverrides    map[string]filterx.SQLHandler
	channelGQLOverrides     map[string]filterx.GraphQLHandler
	unitMappingGQLOverrides map[string]filterx.GraphQLHandler
}

// Option customizes the adapters a factory builds.
type Option func(*options)

// WithChannelHooks installs h on the Channel adapters.
func WithChannelHooks(h ChannelHooks) Option {
	return func(o *options) { o.channelHooks = h }
}

// WithChannelListOverride substitutes the generated filter dispatch for one
// Channel filter field (e.g. a derived state computed via subqueries).
func WithChannelListOverride(field string, h filterx.SQLHandler) Option {
	return func(o *options) {
		if o.channelOverrides == nil {
			o.channelOverrides = map[string]filterx.SQLHandler{}
		}
		o.channelOverrides[field] = h
	}
}

// WithUnitMappingHooks installs h on the UnitMapping adapters.
func WithUnitMappingHooks(h UnitMappingHooks) Option {
	return func(o *options) { o.unitMappingHooks = h }
}

// WithUnitMappingListOverride substitutes the generated filter dispatch for one
// UnitMapping filter field (e.g. a derived state computed via subqueries).
func WithUnitMappingListOverride(field string, h filterx.SQLHandler) Option {
	return func(o *options) {
		if o.unitMappingOverrides == nil {
			o.unitMappingOverrides = map[string]filterx.SQLHandler{}
		}
		o.unitMappingOverrides[field] = h
	}
}

// WithChannelGraphQLListOverride substitutes the generated filter dispatch
// for one Channel filter field on the GraphQL adapters.
func WithChannelGraphQLListOverride(field string, h filterx.GraphQLHandler) Option {
	return func(o *options) {
		if o.channelGQLOverrides == nil {
			o.channelGQLOverrides = map[string]filterx.GraphQLHandler{}
		}
		o.channelGQLOverrides[field] = h
	}
}

// WithUnitMappingGraphQLListOverride substitutes the generated filter dispatch
// for one UnitMapping filter field on the GraphQL adapters.
func WithUnitMappingGraphQLListOverride(field string, h filterx.GraphQLHandler) Option {
	return func(o *options) {
		if o.unitMappingGQLOverrides == nil {
			o.unitMappingGQLOverrides = map[string]filterx.GraphQLHandler{}
		}
		o.unitMappingGQLOverrides[field] = h
	}
}

// New picks the adapter set for the live handle in conn: GraphQL when a
// client connection is present, otherwise GORM.
func New(conn repox.Conn, opts ...Option) Repositories {
	if conn.GraphQL != nil {
		return NewGraphQL(conn.GraphQL, opts...)
	}
	return NewGorm(conn.Gorm, opts...)
}

// NewGraphQL builds the GraphQL adapter set over svc.
func NewGraphQL(svc *freebusyql.Service, opts ...Option) Repositories {
	var o options
	for _, opt := range opts {
		opt(&o)
	}
	return Repositories{
		Channels:     &GraphQLChannelRepository{Svc: svc, Hooks: o.channelHooks, ListOverrides: o.channelGQLOverrides},
		UnitMappings: &GraphQLUnitMappingRepository{Svc: svc, Hooks: o.unitMappingHooks, ListOverrides: o.unitMappingGQLOverrides},
	}
}

// NewGorm builds the GORM adapter set over db.
func NewGorm(db *gorm.DB, opts ...Option) Repositories {
	var o options
	for _, opt := range opts {
		opt(&o)
	}
	return Repositories{
		Channels:     &GormChannelRepository{DB: db, Hooks: o.channelHooks, ListOverrides: o.channelOverrides},
		UnitMappings: &GormUnitMappingRepository{DB: db, Hooks: o.unitMappingHooks, ListOverrides: o.unitMappingOverrides},
	}
}
